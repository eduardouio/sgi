from django.test import TestCase

class CompletePaidInfoTC(TestCase):
    """tests de prueba
    
    Arguments:
        Al momento se lo deja en blanco para que se pueda verificar al correr el test
    """

    def test_hello(self):
        self.assertEqual(1,1)